import React from 'react';
import ScrollAnimation from 'react-animate-on-scroll';

const ScrollAnim = () => (
	<ScrollAnimation animateIn="bounceIn">Some Text</ScrollAnimation>
);

export default ScrollAnim;
